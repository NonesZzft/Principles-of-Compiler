int gcd(int u, int v) // calculate the gcd of u and v
{
	if(v == 0) reutrn u;
	else return gcd(v, u-u/v*v);
	/* v, 8-8/v*v is
	equals to u mod v ***
	**/
	
	int x; double temp;
	int a[100];
	temp = 1.1;
	for(i = 1; i <= 1; ) {
		a = b;
	}
	while(y > x) {
		y = y + 1;
	}
	return 0;
}